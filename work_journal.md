# 작업 일지 (Work Journal)

---

## 📅 2026.07.08

오늘 작업한 소셜 로그인 데이터 복원, 스케일 보정 편의성 개선, 그리고 Supabase Storage 연동 견적 저장 등 백엔드/프론트엔드 핵심 기능 연동 내역을 정리했습니다.

### 1. 완료한 작업 (Accomplished Tasks)

#### 🔄 소셜 로그인(OAuth) 시 도면 유실 방지 및 자동 복원
* **임시 데이터 세션 보관**: 구글 소셜 로그인 등 리다이렉트가 발생하는 인증 진행 시 작성 중이던 도면 정보와 이미지가 유실되지 않도록 `sessionStorage`(`temp_project_oauth`)에 임시 저장하는 구조 구현.
* **로그인 후 복원 로직**: 로그인 후 온보딩 단계 완료 시, 저장된 임시 도면 데이터가 존재할 경우 이를 감지하여 자동으로 복구 및 화면에 로드하는 `checkAndRestoreOauthTempProject` 함수 구현.

#### 📐 스케일 보정(Calibration) 및 진입 단계 레이아웃 개선
* **탑바 비노출 제어**: 도면 업로드 및 스케일 보정이 완료되기 전에는 상단 메뉴 및 로그인/회원가입 버튼이 표시되지 않도록 `.topbar` 숨김 제어 함수(`setTopbarVisible`) 추가.
* **기존 보정 데이터 보존**: 스케일 보정 모드에 재진입하더라도 이전에 사용자가 생성해둔 기준선(2개의 포인트)과 입력했던 수치(기본값 0.9m)를 유지하도록 변경하여 재작업의 피로도 감소.
* **업로드 오버레이와 다운로드 컨테이너 동기화**: 업로드 창 표출 시 우측 상단 다운로드 버튼 컨테이너(`.download-container`)가 함께 숨겨지도록 정교하게 매핑.

#### 📊 Supabase 연동 견적 저장 및 엑셀 스토리지 업로드
* **엑셀 파일 Storage 업로드**: 프로젝트를 저장할 때, 생성된 견적서 엑셀 파일을 Supabase Storage의 `estimates` 버킷 내 사용자 ID 폴더(`{user_id}/{timestamp}_{project_name}.xlsx`)에 즉시 업로드하고 다운로드 URL(`excel_url`)을 DB에 자동 매칭 저장.
* **실시간 도면 썸네일(Base64) 덤프**: 저장 시점의 도면 상태를 임시 캔버스에 렌더링하고 `base64Image`로 변환하여 메타데이터와 함께 저장함으로써 견적 리스트 조회 시 썸네일을 제공할 수 있는 기반 마련.
* **기존 견적 덮어쓰기**: 복원 시 `state.activeQuoteId`를 바인딩하여 새로 저장이 아닌 기존 견적 정보를 업데이트하는 덮어쓰기 기능 고도화.

#### 🛠️ 도면 편집 도구 및 견적 내보내기 필터링 개선
* **공간(Zone) 선택 방지**: 셀렉트 도구(Select Tool) 활성화 상태에서 공간 다각형 영역이 클릭되어 조명 조작에 혼선을 주지 않도록, 선택 대상에서 공간(Zone)은 제외하고 조명(Fixture)만 포커스되도록 수정.
* **자동 매칭 자재 필터링**: 실 견적서의 가독성과 직관성을 확보하기 위해 자동 배정되는 자석 액세서리(Magnetic accessories) 및 2M/3M 트랙 레일류 등을 엑셀 견적 및 화면 테이블 출력에서 필터링하도록 처리.

#### 🔐 프로필 편집 및 비밀번호 변경 프로바이더 분기
* **계정 타입 판별**: 사용자의 `app_metadata` 또는 `identity` 정보를 분석하여 소셜 가입자(Google 등)와 이메일 가입자를 식별.
* **프로필 설정 노출 분기**: 소셜 로그인 유저의 경우 프로필 팝업 상단에 `OAuth` 뱃지를 표시하고 비밀번호 변경을 시도할 수 없도록 사전에 차단 및 분기 처리. 이메일 가입 유저에게만 비밀번호 변경 오버레이 노출.

#### 🖱️ 헤더 메뉴 및 아이콘 정합성 버그 수정
* **햄버거 드롭다운 무반응 버그 해결**: "홈으로 / 파일 저장 / 파일 열기" 3개 메뉴가 UI 개편 과정에서 삭제된 구버전 버튼 ID(`btnNewProject` 등)를 프록시 호출하고 있어 완전히 무반응 상태였던 것을 확인, 실제 로직을 각 메뉴 핸들러에 직접 연결해 정상화.
* **견적 다운로드 아이콘 교체 및 사이즈 튜닝**: 최신 Figma 에셋(`Paper Download.svg`)으로 topbar 버튼·햄버거 메뉴 아이콘 2곳 모두 교체, 스트로크 두께 및 크기를 반복 조정.
* **프로필 아바타 아이콘 교체**: `.profile-avatar`를 `mypage.svg`(원형 배경 내장형)로 교체, 기존 div 배경 스타일과 중복되지 않도록 정리.
* **로그아웃 메뉴 스타일 정리**: 아이콘 제거, 텍스트 컬러 `#737373` 지정 및 가운데 정렬 적용.

#### 🪟 모달 레이아웃 Figma 정합화
* **프로필 모달 닫기 버튼 겹침 해결**: `#profileEditOverlay`의 상단 padding을 Figma 스펙(90px)에 맞춰 늘려, 구글/이메일 헤더 뱃지가 닫기 버튼과 겹치던 문제 해결.
* **비밀번호 변경 모달 신규 구현 (Figma 44:517)**: 기존엔 `confirm()` + 이메일 재설정 링크 방식이었던 것을, 현재 비밀번호 / 새 비밀번호 / 새 비밀번호 확인 3단 입력 폼으로 교체. `signInWithPassword`로 현재 비밀번호를 먼저 검증한 뒤 `updateUser`로 실제 변경하도록 구현.
* **의견 남기기 모달 리스트뷰 추가 (Figma 80:1012)**: 기존엔 이전 피드백 이력이 항상 인라인으로 노출되던 것을, "내가 남긴 의견 보기" 클릭 시에만 별도 리스트뷰로 전환되도록 구조 변경.
* **피드백 텍스트박스 도형 불일치 수정**: `.auth-card .form-input` 공통 스타일(60px 높이 / 알약형 radius)이 다중행 textarea에도 강제 적용되던 버그를 발견, Figma 노드(54:739) 수치(199px, radius 6px, padding 18.5/16px)로 스코프 override.

#### 🐛 회원가입 폼 안정성 버그 수정
* **자동완성 시 가입 버튼 비활성 문제 해결**: 브라우저/비밀번호 관리자가 자동완성한 값은 `input` 이벤트가 안정적으로 발생하지 않아 버튼이 계속 비활성 상태로 남는 문제를 확인, `change`/`blur` 이벤트 추가 및 500ms 폴링으로 보강.
* **회원가입 모달 닫기 시 폼 초기화**: 닫기(X) 클릭 시 입력 필드 및 동의 체크박스가 초기화되도록 추가.
* **이메일 가입자 프로필 정보 미표시 버그 해결**: `signUp()` 직후 `onAuthStateChange`의 프로필 조회가 실제 upsert보다 먼저 실행되는 레이스 컨디션으로 인해, DB엔 정상 저장돼도 화면엔 이름/업체명이 빈 값으로 보이던 문제를 발견. upsert 성공 직후 `authProfile`을 메모리에서 즉시 갱신하도록 수정 (구글 온보딩 플로우에 이미 있던 패턴을 이메일 가입 플로우에도 동일 적용).

#### 🔐 Supabase 권한(RLS) 전수 점검 및 수정
* **`feedbacks` 테이블 부재 발견 및 생성**: "의견 남기기" 기능이 참조하는 테이블이 아예 존재하지 않아 모든 제출이 조용히 실패하고 있던 것을 발견, 테이블 신규 생성 및 본인 글만 select/insert 가능한 RLS 정책 추가.
* **`quotes` / `quote_items` RLS 정책 부재 수정**: RLS는 켜져 있으나 정책이 하나도 없어 "내 견적서" 저장/불러오기/삭제가 전부 막혀있던 것을 확인, `user_id = auth.uid()` 기준 select/insert/update/delete 정책을 각각 추가.
* **`estimates` 스토리지 버킷 업로드 실패 원인 규명 및 해결**: 견적 저장 시 엑셀 파일 업로드가 400 에러로 조용히 실패해 `excel_url`이 항상 비어있던 문제를, 로그 분석으로 정확한 실패 요청을 특정. 기존의 지나치게 느슨했던 전역 업로드 정책과 별개로 본인 폴더 기준 insert/update/delete + 공개 read 정책을 `storage.objects`에 추가하여 해결 (재테스트로 `excel_url` 정상 저장 검증 완료 → 아래 "다음 작업 사항" 1번 완료 처리).
* **Auth 이메일 인증 정책 조정**: 초기 서비스 단계에 맞춰 Supabase 대시보드에서 "Confirm email"을 비활성화, 가입 즉시 로그인되도록 안내 및 적용.

#### 🖱️ 캔버스 편집 편의 기능 추가
* **햄버거 메뉴 "스케일 보정" 항목 신설**: "홈으로" 바로 아래에 메뉴 추가, 클릭 시 기존 `startCalibrationFlow()`를 재호출하여 도면 배치 화면에서도 스케일 보정 화면으로 즉시 재진입 가능하도록 구현.

### 2. 작업 브랜치 정보 (Git Branch Information)
* **브랜치**: `feature/ui-refinements` (작업 내용 로컬 스테이징 상태)
* **최근 커밋**: `feat: implement figma-spec ui refinements and supabase products excel export`

### 3. 다음 작업 사항 (Next Steps)
1. ~~Supabase Storage 업로드 권한(RLS) 확인~~ → **완료**: `estimates` 버킷 정책 추가 및 실 업로드 테스트로 검증 완료.
2. **소셜 로그인 복원 후 세션 클리어링**: `temp_project_oauth` 복원이 완료된 후 불필요한 스토리지 클리어 검증.
3. **불러온 견적 덮어쓰기 UI 피드백**: 저장 시 사용자에게 '새로 저장'과 '기존 견적에 덮어쓰기' 중 선택할 수 있도록 옵션 제공 검토.
4. **개인정보처리방침 페이지 작성**: Google OAuth 동의 화면 브랜딩에 비어있는 개인정보처리방침 링크 채워넣기 (공개 서비스 전환 시 권장).
5. **Google Identity Services 직접 연동 검토**: 현재 Supabase 기본 OAuth 흐름에서는 구글 로그인 시 계정 선택 화면에 앱 도메인 대신 `*.supabase.co`가 노출됨. Free 플랜에서 Custom Domain을 쓸 수 없으므로, 필요 시 ID 토큰 방식(`signInWithIdToken`) 직접 연동으로 전환 검토.
6. **Leaked Password Protection 재검토**: HaveIBeenPwned 연동 기능은 Pro 플랜부터 지원 — 유료 플랜 업그레이드 시 활성화.

---

## 📅 2026.07.07

오늘 작업한 피그마 디자인 매칭 및 기능 고도화 내역을 정리했습니다. 내일 이어서 작업을 원활하게 진행할 수 있도록 공유합니다.

---

## 1. 오늘 완료한 작업 (Accomplished Tasks)

### 🎨 UI & 레이아웃 정밀 개선
* **견적 다운로드 버튼 (`.btn-download`) 디자인 교체 (Figma 45:410)**:
  - 캡슐 알약 형태(`border-radius: 60px`), 높이 `34px`, 간격 `gap: 8px`, 폰트 `12px Pretendard Medium` 적용.
  - 내부에 피그마 고유 다운로드 SVG 아이콘(`34:408`)을 획두께 `1.2`로 얇게 이식.
  - 마우스 호버 시 배경색 `#00549B`로 채도 변경 연동.
* **상단 액션 컨테이너 여백 조정**:
  - 우측 헤더 메뉴의 밀착 배치를 위해 `.topbar-actions` 클래스 및 인라인 스타일의 `gap` 속성을 모두 `0px`로 변경.
* **다운로드 툴팁 말풍선 스타일 리팩토링 (`.download-tooltip`)**:
  - 인라인 스타일을 클래스로 전환하여 공통 스타일시트(`style.css`)에 이식.
  - 피그마 규격대로 가로폭 `260px`, 글자 크기 `11px`, 글자 색상 `rgb(182, 182, 182)` 조정.
* **안내 정보 아이콘 (`.info-btn`) 정밀 축소 및 에셋 연동**:
  - `img/i.svg` 파일을 정적으로 참조하도록 수정하고 크기를 `18px x 18px`로 수동 비례 조정하여 우측 정렬 완료.

### 🎬 프리미엄 모션 애니메이션 구현
* **모달 팝업 페이드인 & 슬라이드 리빌 효과 구현**:
  - **오버레이 백드롭**: `opacity: 0` → `1` 및 `backdrop-filter: blur(8px)`를 동시에 처리하여 화면 흐림(Blur) 효과가 모바일/데스크톱 모두에서 뚝뚝 끊기지 않고 부드럽게 감속되며 밀려오도록 0.5초 동안 활성화.
  - **팝업 본체 카드**: 아래에서 위로 `translateY(50px)`에서 `0px`로 서서히 솟아오르며 투명도가 `0.7`에서 `1.0`으로 흐르는 0.6초 구간의 모션 적용.
  - **감속 가속도**: 자연스러운 가속 및 완만한 감속을 지원하는 `cubic-bezier(0.25, 1, 0.5, 1)` 매핑.

### 🔐 회원가입/로그인 카드 고도화 (Figma 21:394)
* **팝업 카드 리디자인**: 가로폭 `505px`, 모서리 라운딩 곡률 `40px`, 배경색 `#1e1e1e`로 교체.
* **구글 로그인 버튼**: 크기 `60px` 높이, 곡률 `40px`, 배경 `#2a2a2a`로 적용 및 마우스 호버 시 **배경 화이트 / 글씨 블랙** 반전 모션 추가.
* **입력 필드**: 라벨을 숨기고 인풋 박스를 높이 `60px`, 패딩 좌우 `34px`, 곡률 `40px`로 교체.
* **비밀번호 토글 아이콘 교체 (Figma 60:875 & 60:878)**:
  - 기존 🔒/👁️ 이모지 텍스트를 제거하고, 피그마 규격의 선형 SVG 아이콘(눈동자 눈 모양 & 사선 그어진 눈 모양)으로 동적 토글 렌더링되도록 `setupPasswordToggle` 함수 개선.
* **동적 버튼 활성화**:
  - 필수 필드 입력 및 가입 동의 약관 체크박스(커스텀 `22px`로 적용 완료)가 완료되면, 회원가입 버튼이 즉시 활성화되도록 real-time input event listener 바인딩.
  - 활성화 시 회색 비활성 상태에서 피그마 지정 블루 컬러(`#036cc5` 배경, `#ffffff` 텍스트)로 자동 전환.

### ✖️ 34px 원형 공통 닫기 버튼 탑재 (Figma 44-476)
* 팝업 테두리 기준 `top: 20px, right: 20px` 여백에 위치한 **`34px x 34px` 크기의 원형 닫기 단추(`.modal-close-btn`)** 디자인을 전면 적용.
* 내부 엑스(`X`) 모양 라인 SVG 포함 및 배경색 `#2a2a2a` (호버 시 `#3a3a3a`) 결합.
* 기존 회원 인증 모달 4종은 물론 경고 대화상자(`#confirmOverlay`), 구역 타입 선택창(`#zoneSelectOverlay`), 스위치 설정창(`#switchInputOverlay`)까지 총 6개 팝업에 전부 추가 완료.

### 📊 데이터 엑셀 내보내기 자동화
* **수파베이스 제품 연동 스크립트 작성**:
  - `products` 테이블에서 30개 제품 전체 목록을 fetch하여 엑셀 파일로 출력하는 Python 스크립트(`scratch/export_products.py`) 개발 및 실행.
  - 다운로드 폴더에 **`/Users/zibis/Downloads/supabase_products.xlsx`** 생성 완료.

---

## 2. 작업 브랜치 정보 (Git Branch Information)

* **생성된 새 브랜치**: `feature/ui-refinements`
* **커밋 메시지**: `feat: implement figma-spec ui refinements and supabase products excel export`
* **푸시 완료**: `origin feature/ui-refinements` (Main에 머지하지 않음)

---

## 3. 내일 이어서 작업할 사항 (Next Steps / Tomorrow's Checklist)

1. **로그인 상태 분기 및 회원 정보 로드 검증**:
   - 가입 완료 후 Supabase Auth 상태 연동 시 회원 정보 수정 모달 및 온보딩 대화상자의 추가 데이터 바인딩 정상 작동 모니터링.
2. **반응형 테블릿/모바일 뷰 최적화**:
   - 팝업 너비(`505px`)가 너무 좁은 모바일 브라우저 환경에서 깨지지 않고 `max-width: 95vw` 분기로 흘러가도록 미디어 쿼리 튜닝 검토.
3. **스냅 및 도면 캔버스 연동 기능 피드백 수집**:
   - 수평 정렬 보정된 스냅 스위치 토글 기능의 추가 사용성 검토 및 최종 사용자 릴리즈 대비.
